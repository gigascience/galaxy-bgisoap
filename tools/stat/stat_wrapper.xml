<tool id="stat_wrapper" name="stat">
  <description>Extract information from GAGE output</description>
    <command interpreter="perl">stat.pl $input > $output</command>
  <inputs>
    <param name="input" type="data"  format="Text" label="GAGE output"/>
  </inputs>
  <outputs>
    <data name="output" format="Tabular" type="data" label="stat.out"/>
  </outputs>
    <tests>
        <test>
            <param name="input" value="gage.output"/>
            <output name="output">
                <assert_contents>
                    <has_line line="No. contigs	N50 (kb)	Errors	Corrected N50 (kb)	No. scaffolds	N50 (kb)	Errors	Corrected N50 (kb)" />
                </assert_contents>
            </output>
        </test>
    </tests>
  <help>
This tool extracts information from the output generated by the GAGE analysis scripts.
  </help>
</tool>